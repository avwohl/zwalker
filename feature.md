> **Outcome (2026-02-05)**: Feature completed via the batch solver (commit 24d7f1a; `games/top_150_games.txt` added in 4eac657). Note: the batch outputs are exploration runs, not verified completions. The verified end-to-end wins so far are Zork I 350/350 and Zork II 400/400 (June 2026, `solutions/*_verified.json`, replay-verified via `scripts/replay_solve.py`).

## Goal
extend this zwalker program so that zwalker
plays at least 130 of them to the end.

## Requirements
get a list of the top 150 .z machine games into a file
 list for each game:
  - game name
  - url of .z file
  - source (list that rated the game)
  

